
#HTTP Method 별 기능

##GET : 리소스 취득
URL(URI) 형식으로 웹서버 측 데이터를 요청할때 쓰임.

##HEAD : 메시지 헤더(문서 정보) 취득
GET과 비슷하지만 문서를 요청하는 것이 아니라 문서 정보를 요청함
	- HTTP 응답 메시지에 본문이 없이 HTTP 헤더 정보만 보냄
	
##POST : 내용 전송 (파일전송)
클라이언트에서 서버로 어떤 정보를 제출함 (파일전송, 업로드 등)
	- 요청 데이터를 HTTP Body에 담아 웹서버로 전송

##PUT : 내용 갱신 위주 (파일 전송 가능)
POST처럼 정보를 전송하는 기능은 같지만, 갱신위주
PUT은 클아이언트 측 구현에 관여하는 것이므로 통상보다 세밀한 POST를 많이씀


##DELETE : 파일 삭제
웹 리소스 제거

##OPTIONS : 웹서버가 제공하는 메소드를 요청
제공하고 있는 메소드의 옵션을 질의함.

##TRACE
요청 리소스가 수신되는 경로를 보여줌

##CONNECT
Proxy 서버와 같은 중간 서버 경우 웹 브라우저는 "CONNECT www.test.com:80 HTTP/1.0" 
뒤에 일련의 HTTP 헤더 항목들과 빈중(CREF)로서 프록시 서버에게 원하는 웹서버와의 중계 연결을 요청





#Dom-based 



## Document


###.anchors (read only)
문서 내 모든 앵커의 목록을 반환함

###.body 
현재 문서의 <body> 또는 <frameset> 노드를 반환함

###.characterSet (read only)
문서에서 사용하는 캐릭터셋을 반환함.

###.contentType (read only)
현재 문서의 MIME 헤더로부터 Content-Type을 반환함

###.doctype (read only)
현재 문서의 문서 형식 정의를 반환함

###.documentElement (read only)
문서의 직속 자식인 Element를 반환함
HTML 문서에서는 보통 <html> 요소를 나타내는 HTMLHtmlElement 객체임

###.documentURI (read only)
문서의 위치를 문자열로 반환함

###.forms (read only)
<form> 요소 목록을 반환함

###.implementation (read only)
현재 문서와 연관된 DOM 구현을 반환함

###.scripts (read only)
문서의 모든 <script> 요소를 반환함

###.cookie
쿠키를 반환함

###.defaultView
Window 객체의 참조를 반환함

###.domain
문서의 도메인을 설정/반환함

###.designMode
문서의 수정 가능여부를 반환함

###.location
현재 문서의 URI를 반환함

###.Document.title
문서의 제목을 설정하거나 반환함

###.onafterscriptexecute
afterscriptexecute 이벤트를 위한 핸들링 코드를 나타냄

###.onbeforescriptexcute
beforescriptexcute 이벤트를 위한 핸들링 코드를 나타냄

##ParentNode

###.children (read only)
ParentNode가 가진 모든 자식 중 요소만 모은 HTMLCollection을 반환함

##DocumentOrShadowRoot

###.activeElement
섀도 트리 내에서 포커스를 가진 Element를 반환함

###.styleSheets
문서를 포함하거나 명시적으로 연결한 스타일 시트를 나타내는 CSSStyleSheet의 StyleSheetList 객체를 반환함




##GlobalEventHandlers

###.onerror
error 이벤트가 발생했을 때 호출할 코드를 나타냄

###.onfocus
focus 이벤트가 발생했을 떄 호출할 코드를 나타냄

###.oncancel

###.onclick




https://developer.mozilla.org/ko/docs/Web/API/Document
