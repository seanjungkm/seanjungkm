
### XSS Detection in python

from flask import Flask, request, make_response, escape
 
app = Flask(__name__)
 
@app.route('/unsafe')
def unsafe():
    first_name = request.args.get('name', '')
    return make_response("Your name is " + first_name)
 
@app.route('/safe')
def safe():
    first_name = request.args.get('name', '')
    return make_response("Your name is " + escape(first_name))
	
	
	
	escape 처리를 통해 입력되는 변수를 단순 문자열만 받도록 지정
	
	참조 https://help.semmle.com/wiki/display/PYTHON/Reflected+server-side+cross-site+scripting
	